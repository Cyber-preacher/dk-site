{
	# Set via docker-compose environment. Email is optional but recommended.
	email {$EMAIL}
}

{$DOMAIN}, www.{$DOMAIN} {
	@www host www.{$DOMAIN}
	redir @www https://{$DOMAIN}{uri} permanent

	@unsafe_methods method POST PUT PATCH DELETE
	respond @unsafe_methods "Method not allowed" 405

	@next_action header Next-Action *
	respond @next_action "Not found" 404

	encode zstd gzip

	header {
		# Enable HSTS once you're confident the domain is correct and stable.
		# Strict-Transport-Security "max-age=15552000; includeSubDomains"
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
	}

	reverse_proxy app:3000
}
